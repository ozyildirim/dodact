import 'package:flutter/material.dart';

class CreatorAgreementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Dodact İçerik Üretici Sözleşmesi"),
      ),
    );
  }
}
