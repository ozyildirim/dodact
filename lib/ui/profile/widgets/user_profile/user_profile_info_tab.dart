import 'package:flutter/material.dart';

class UserProfileInfoTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        //
        //TODO: İçerik doldurulacak
      ],
    );
  }
}
